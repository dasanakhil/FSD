/* =====================================
   AURA 3D MUSIC PLAYER
===================================== */

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --bg: #050509;
  --panel: rgba(255, 255, 255, 0.04);
  --border: rgba(255, 255, 255, 0.09);
  --text: #ffffff;
  --muted: #858592;
  --accent: #a970ff;
  --accent2: #5b7cff;
}

body {
  min-height: 100vh;
  font-family: Arial, Helvetica, sans-serif;
  color: var(--text);

  background:
    radial-gradient(circle at 50% 25%, #171126, transparent 35%),
    radial-gradient(circle at 10% 80%, #0c1630, transparent 35%),
    #050509;

  overflow-x: hidden;
}


/* BACKGROUND */

.noise {
  position: fixed;
  inset: 0;

  opacity: 0.025;

  background-image:
    repeating-linear-gradient(
      45deg,
      white 0,
      white 1px,
      transparent 1px,
      transparent 4px
    );

  pointer-events: none;
}

.glow {
  position: fixed;
  width: 500px;
  height: 500px;

  border-radius: 50%;
  filter: blur(150px);

  opacity: 0.12;
  pointer-events: none;
}

.glow-1 {
  background: var(--accent);
  top: -200px;
  left: -150px;
}

.glow-2 {
  background: var(--accent2);
  right: -200px;
  bottom: -200px;
}


/* APP */

.app {
  width: min(1350px, 94%);
  margin: auto;
  position: relative;
  z-index: 2;
}


/* HEADER */

header {
  min-height: 100px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  border-bottom: 1px solid var(--border);
}

.brand {
  display: flex;
  align-items: center;
  gap: 14px;
}

.brand-icon {
  width: 48px;
  height: 48px;

  display: grid;
  place-items: center;

  border-radius: 14px;

  font-weight: 900;
  font-size: 21px;

  background:
    linear-gradient(
      135deg,
      var(--accent),
      var(--accent2)
    );

  box-shadow:
    0 0 40px rgba(169, 112, 255, 0.3);
}

.brand h1 {
  letter-spacing: 5px;
  font-size: 18px;
}

.brand span {
  color: var(--muted);
  font-size: 8px;
  letter-spacing: 3px;
}

.status {
  color: var(--muted);

  font-size: 9px;
  letter-spacing: 2px;

  display: flex;
  align-items: center;
  gap: 8px;
}

.status-dot {
  width: 7px;
  height: 7px;

  background: #73ff9b;
  border-radius: 50%;

  box-shadow: 0 0 15px #73ff9b;
}


/* HERO */

.hero {
  min-height: 500px;

  display: grid;

  grid-template-columns:
    1fr
    1.4fr
    0.8fr;

  align-items: center;

  border-bottom: 1px solid var(--border);
}

.eyebrow {
  color: var(--accent);

  font-size: 9px;

  letter-spacing: 3px;

  font-weight: bold;

  margin-bottom: 15px;
}

.hero-info h2 {
  font-size: clamp(45px, 5vw, 78px);

  line-height: 0.95;

  letter-spacing: -4px;
}

.hero-info h2 span {
  display: block;

  color: transparent;

  -webkit-text-stroke:
    1px rgba(255,255,255,0.55);
}

.description {
  max-width: 350px;

  margin-top: 25px;

  color: var(--muted);

  font-size: 12px;

  line-height: 1.8;
}


/* STATS */

.mini-stats {
  display: flex;

  gap: 35px;

  margin-top: 40px;
}

.mini-stats div {
  display: flex;
  flex-direction: column;
}

.mini-stats strong {
  font-size: 20px;
}

.mini-stats span {
  margin-top: 4px;

  color: var(--muted);

  font-size: 7px;

  letter-spacing: 2px;
}


/* =====================================
   3D STAGE
===================================== */

.visual-stage {
  height: 440px;

  position: relative;

  display: flex;
  align-items: center;
  justify-content: center;

  perspective: 1000px;

  overflow: hidden;
}


/* ORBITS */

.orbit {
  position: absolute;

  border:
    1px solid rgba(255,255,255,0.1);

  border-radius: 50%;
}

.orbit-one {
  width: 330px;
  height: 330px;

  animation:
    orbitRotate 13s linear infinite;
}

.orbit-two {
  width: 390px;
  height: 190px;

  transform: rotateX(70deg);

  animation:
    orbitRotate 9s linear infinite reverse;
}

.orbit-three {
  width: 260px;
  height: 400px;

  transform: rotateY(70deg);

  opacity: 0.45;

  animation:
    orbitRotate 17s linear infinite;
}

@keyframes orbitRotate {

  from {
    rotate: 0deg;
  }

  to {
    rotate: 360deg;
  }
}


/* CUBE */

.cube-wrapper {
  width: 220px;
  height: 220px;

  perspective: 900px;

  z-index: 5;
}

.album-cube {
  width: 220px;
  height: 220px;

  position: relative;

  transform-style: preserve-3d;

  transform:
    rotateX(-8deg)
    rotateY(-15deg);

  transition:
    transform 0.15s ease-out;
}

.album-cube.playing {
  animation:
    floating 4s ease-in-out infinite;
}

@keyframes floating {

  0%,
  100% {
    translate: 0 0;
  }

  50% {
    translate: 0 -15px;
  }
}

.cube-face {
  position: absolute;

  width: 220px;
  height: 220px;

  background:
    linear-gradient(
      135deg,
      #16161d,
      #08080d
    );

  border:
    1px solid rgba(255,255,255,0.12);
}

.front {
  transform: translateZ(20px);

  overflow: hidden;

  border-radius: 8px;
}

.front img {
  width: 100%;
  height: 100%;

  object-fit: cover;

  transition:
    transform 0.7s ease,
    filter 0.7s ease;
}

.album-cube.playing .front img {
  transform: scale(1.04);
}

.back {
  transform:
    rotateY(180deg)
    translateZ(20px);
}

.left {
  width: 40px;

  transform:
    rotateY(-90deg)
    translateZ(20px);
}

.right {
  width: 40px;

  right: 0;

  transform:
    rotateY(90deg)
    translateZ(20px);
}

.top {
  height: 40px;

  transform:
    rotateX(90deg)
    translateZ(20px);
}

.bottom {
  height: 40px;

  bottom: 0;

  transform:
    rotateX(-90deg)
    translateZ(20px);
}


/* SHADOW */

.floor-shadow {
  position: absolute;

  width: 220px;
  height: 30px;

  bottom: 70px;

  background:
    rgba(120,80,255,0.25);

  filter: blur(22px);

  border-radius: 50%;
}


/* PARTICLES */

.particle {
  position: absolute;

  width: 3px;
  height: 3px;

  border-radius: 50%;

  background: white;

  opacity: 0.4;

  animation:
    particleFloat 5s ease-in-out infinite;
}

@keyframes particleFloat {

  0%,
  100% {
    transform: translateY(0);
    opacity: 0.15;
  }

  50% {
    transform: translateY(-25px);
    opacity: 0.8;
  }
}


/* =====================================
   AUDIO PANEL
===================================== */

.audio-panel {
  border-left: 1px solid var(--border);

  padding-left: 30px;
}

.audio-setting {
  padding: 18px 0;

  border-bottom:
    1px solid var(--border);

  display: flex;
  justify-content: space-between;
  align-items: center;
}

.audio-setting span {
  font-size: 11px;

  letter-spacing: 1px;
}

.audio-setting small {
  display: block;

  margin-top: 5px;

  color: var(--muted);

  font-size: 7px;

  letter-spacing: 1px;
}

.meter {
  display: flex;
  gap: 3px;
}

.meter i {
  width: 3px;
  height: 18px;

  display: block;

  background:
    linear-gradient(
      to top,
      var(--accent2),
      var(--accent)
    );

  border-radius: 4px;
}


/* =====================================
   PLAYER
===================================== */

.player {
  padding: 45px 0;

  text-align: center;

  border-bottom:
    1px solid var(--border);
}

.song-meta h3 {
  font-size: 31px;

  margin-bottom: 8px;
}

.song-meta span {
  color: var(--muted);

  font-size: 12px;

  letter-spacing: 2px;
}


/* VISUALIZER */

#visualizer {
  display: block;

  width: min(650px, 90%);
  height: 90px;

  margin: 25px auto;
}


/* PROGRESS */

.progress-section {
  width: min(650px, 90%);

  margin: auto;

  display: grid;

  grid-template-columns:
    45px 1fr 45px;

  align-items: center;

  gap: 15px;

  color: var(--muted);

  font-size: 10px;
}

input[type="range"] {
  width: 100%;

  accent-color: var(--accent);

  cursor: pointer;
}


/* CONTROLS */

.controls {
  margin-top: 30px;

  display: flex;

  justify-content: center;
  align-items: center;

  gap: 20px;
}

.controls button {
  color: white;

  cursor: pointer;

  border: none;

  transition: 0.25s;
}

.control-btn {
  width: 50px;
  height: 50px;

  border-radius: 50%;

  background:
    rgba(255,255,255,0.06);

  border:
    1px solid var(--border) !important;
}

.control-btn:hover {
  transform: scale(1.1);

  background:
    rgba(255,255,255,0.12);
}

.play-btn {
  width: 74px;
  height: 74px;

  border-radius: 50%;

  font-size: 21px;

  background:
    linear-gradient(
      135deg,
      var(--accent),
      var(--accent2)
    );

  box-shadow:
    0 15px 45px
    rgba(140,90,255,0.35);
}

.play-btn:hover {
  transform: scale(1.08);
}

.secondary-btn {
  width: 42px;
  height: 42px;

  border-radius: 50%;

  background: transparent;

  color:
    var(--muted) !important;

  font-size: 18px;
}

.secondary-btn.active {
  color:
    var(--accent) !important;

  background:
    rgba(169,112,255,0.1);
}


/* VOLUME */

.volume {
  width: 250px;

  margin: 25px auto 0;

  display: grid;

  grid-template-columns:
    30px 1fr 35px;

  align-items: center;

  gap: 10px;

  color: var(--muted);

  font-size: 8px;
}


/* =====================================
   PLAYLIST
===================================== */

.playlist-section {
  padding: 45px 0;
}

.playlist-heading {
  display: flex;

  justify-content: space-between;
  align-items: flex-end;

  margin-bottom: 25px;
}

.playlist-heading h3 {
  font-size: 27px;
}

#trackCount {
  color: var(--muted);

  font-size: 9px;

  letter-spacing: 2px;
}

.playlist {
  display: grid;

  grid-template-columns:
    repeat(4, 1fr);

  gap: 15px;
}

.track {
  padding: 12px;

  border-radius: 16px;

  background:
    var(--panel);

  border:
    1px solid var(--border);

  cursor: pointer;

  transition:
    transform 0.3s,
    background 0.3s,
    border 0.3s;
}

.track:hover {
  transform:
    translateY(-7px);

  background:
    rgba(255,255,255,0.07);
}

.track.active {
  border-color:
    rgba(169,112,255,0.6);

  background:
    rgba(169,112,255,0.08);
}

.track img {
  width: 100%;

  aspect-ratio: 1;

  object-fit: cover;

  border-radius: 10px;

  margin-bottom: 15px;
}

.track-number {
  color: var(--accent);

  font-size: 8px;

  letter-spacing: 2px;
}

.track h4 {
  margin-top: 7px;

  font-size: 14px;
}

.track p {
  margin-top: 5px;

  color: var(--muted);

  font-size: 10px;
}


/* FOOTER */

footer {
  min-height: 80px;

  display: flex;

  justify-content: space-between;
  align-items: center;

  color: var(--muted);

  font-size: 7px;

  letter-spacing: 2px;
}


/* =====================================
   RESPONSIVE
===================================== */

@media (max-width: 950px) {

  .hero {
    grid-template-columns:
      1fr 1fr;
  }

  .audio-panel {
    grid-column:
      1 / -1;

    border-left: none;

    border-top:
      1px solid var(--border);

    padding:
      25px 0;
  }

  .playlist {
    grid-template-columns:
      repeat(2, 1fr);
  }
}


@media (max-width: 650px) {

  header {
    min-height: 85px;
  }

  .status {
    display: none;
  }

  .hero {
    display: flex;

    flex-direction: column;

    padding-top: 50px;
  }

  .hero-info {
    width: 100%;
  }

  .hero-info h2 {
    letter-spacing: -2px;
  }

  .visual-stage {
    width: 100%;
    height: 400px;
  }

  .audio-panel {
    width: 100%;
  }

  .playlist {
    grid-template-columns: 1fr 1fr;
  }

  .controls {
    gap: 10px;
  }

  footer {
    flex-direction: column;

    justify-content: center;

    gap: 10px;

    text-align: center;
  }
}


@media (max-width: 430px) {

  .playlist {
    grid-template-columns: 1fr;
  }

  .cube-wrapper,
  .album-cube,
  .cube-face {
    width: 190px;
    height: 190px;
  }

  .orbit-one {
    width: 280px;
    height: 280px;
  }

}